{
    "get_datasources":{

        "sql":"SELECT id,
                      project,
                      dataset,
                      contenttype,
                      host,
                      name,
                      type,
                      active_status,
                      creation_date
               FROM datasource
               WHERE active_status = 1",

            "host":"master_host"
        }
}
