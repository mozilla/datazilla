{
  "inserts":{
      "store_json":{

         "sql":"INSERT INTO `objectstore` (`timestamp`, `json_blob`) VALUES (NOW(), ?)",

         "host":"master_host"
      }
  },
  "selects":{
      "get_unprocessed":{

         "sql":"SELECT `json_blob`,
                        `id`
                FROM `objectstore`
                WHERE processed_flag = 'ready'",

         "host":"master_host"

      }
  }
}

